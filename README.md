# Strava Autopilot

Personal GitHub Actions poller that fixes new runs automatically. Settings are in `autopilot/settings.json`. Secrets required in repo settings:
Schedule: every 5 minutes; exits if outside active hours.
